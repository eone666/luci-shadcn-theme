config system
	option hostname 'openwrt-testbed'
	option timezone 'UTC'
	option ttylogin '0'
	option log_size '64'

# In a container the time comes from the host, ntpd only spams the log.
config timeserver 'ntp'
	option enabled '0'
